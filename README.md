# todo-express

ハンズオンNode.jsの５章のexpressのプログラムをTypeScriptで再構築

---

## 起動方法

```bash
# 依存インストール
npm i express
npm install -D typescript @types/node @types/express

# 開発サーバー起動
npm run start

# アクセス
http://localhost:3000/